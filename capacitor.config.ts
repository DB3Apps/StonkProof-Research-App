import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.stonkproof.research',
  appName: 'StonkProof Research',
  webDir: 'build'
};

export default config;
